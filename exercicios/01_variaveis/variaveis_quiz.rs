// variaveis_quiz.rs
//
// Sem dicas por enquanto :/


fn meus_dados() -> (&'static str, i32, f64) {
    // crie 3 variáveis com o seu `nome`, `idade` e `altura`
    let nome = "John";
    let idade = 18;
    let altura = 1.80;


    // Não modificar a linha abaixo
    (nome, idade, altura)
}




// Ignorar as linhas a baixo
#[cfg(test)]
mod tests {}